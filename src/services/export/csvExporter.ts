export async function exportSessionsCsv(
  sessions: unknown[],
  options?: { dateFrom?: string; dateTo?: string; vehicleId?: string },
): Promise<string> {
  // TODO: generate CSV string from sessions
  return '';
}
