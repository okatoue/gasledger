import { View, Text, StyleSheet } from 'react-native';

export default function TrackingStatusBanner() {
  // TODO: implement tracking status banner
  return (
    <View style={styles.container}>
      <Text>TrackingStatusBanner (placeholder)</Text>
    </View>
  );
}

const styles = StyleSheet.create({ container: { padding: 12 } });
